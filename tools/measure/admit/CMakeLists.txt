cmake_minimum_required(VERSION 3.20)
project(runD_measure_admit LANGUAGES CXX)

# Admission executes the freshly installed measurement routes.  This configure
# boundary authenticates that package and supplies the compiler provenance for
# the aggregate evidence packet; it deliberately owns no second workload.
include("${CMAKE_CURRENT_LIST_DIR}/../package/installed.cmake")
