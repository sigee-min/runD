# Math32 RL Contract

Math32 RL owns vector recurrence helpers: `Bellman`, `TdError`, `QUpdate`,
`ReturnStep`, and `GaeStep`. These compose fixed multiply and saturating
arithmetic from the math32 SIMD lane law.

Element scalar values are storage and test literals only; they are not public
formula contracts or fallback modes.

Evidence: `math32.contract`.
